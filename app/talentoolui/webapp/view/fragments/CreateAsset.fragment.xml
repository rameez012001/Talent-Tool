<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core">

    <Dialog
        id="assetDialog"
        title="Create User"
        draggable="true"
        resizable="true">

        <VBox class="sapUiSmallMargin">

            <Label text="User ID"/>
            <Input
                id="assetName"
                placeholder="Enter ID"/>

            <Label text="Name"/>
            <Input
                id="assetId"
                placeholder="Enter Name"/>

            <Label text="Email"/>
            <Input
                id=""
                placeholder="Enter Email"/>
            
            <Select id="assetStatus" selectedKey="AVAILABLE">
                <items>
                    <core:Item text="AVAILABLE" key="AVAILABLE"/>
                    <core:Item text="IN_USE" key="IN_USE"/>
                    <core:Item text="MAINTENANCE" key="MAINTENANCE"/>
                    <core:Item text="RETIRED" key="SDE"/>
                </items>
            </Select>
            
        </VBox>

        <beginButton>
            <Button
                text="Create"
                type="Emphasized"
                press="onCreateUser"/>
        </beginButton>

        <endButton>
            <Button
                text="Cancel"
                press="onCloseDialog"/>
        </endButton>

    </Dialog>

</core:FragmentDefinition>
